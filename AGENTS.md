# customer-work 项目工作手册（AI 会话用）

**本文件的全部内容已合并到 [`CLAUDE.md`](CLAUDE.md)，请直接阅读那一份。**

## 为什么改成指针

这两份文件曾经是同一份内容的两个副本（结构逐节相同、正文 97% 重合），由不同的 AI 工具各自读取。
两份副本各自演进的结果是：`AGENTS.md` 落后 `CLAUDE.md` 约 130 行，缺失 B7 主体配额、数据权限、
`admin.tenant.enabled` 默认翻转等**已经生效的行为约定**——而读到旧副本的人，会按一套过时的规则改代码。

同源文档的维护成本不在写，在于**每次改动都要记得改两处**；漏改不会报错，只会让其中一份悄悄变成错的。
与其维护两份，不如只留一份权威来源。

## 各工具的读取约定

| 工具 | 读取文件 | 说明 |
|---|---|---|
| Claude Code | `CLAUDE.md` | 权威来源，所有项目约定写在这里 |
| 其它读取 `AGENTS.md` 的工具 | 本文件 → `CLAUDE.md` | 顺着这个指针读过去即可 |

本机专属信息（容器、凭据、IDE 坑）在 `CLAUDE.local.md`（gitignored，不进仓库）。

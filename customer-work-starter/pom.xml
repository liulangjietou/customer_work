<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>io.github.richardfyoung</groupId>
        <artifactId>customer-work-parent</artifactId>
        <version>2.2.0</version>
        <relativePath>../pom.xml</relativePath>
    </parent>

    <artifactId>customer-work-starter</artifactId>
    <packaging>jar</packaging>
    <name>customer-work-starter</name>
    <description>可复用的 AgentScope 智能体能力：模型层/记忆/RAG/工具SPI/会话/可观测/安全/Nacos 等</description>

    <properties>
        <!-- SFTP 客户端：JSch 官方已停更，改用社区维护版 mwiede/jsch（同 com.jcraft.jsch 包 API）；
             版本只在本模块声明，不动根 pom。 -->
        <jsch.version>0.2.23</jsch.version>
    </properties>

    <dependencies>
        <!-- WebFlux + 校验：Filter/Reactor 能力（接入层安全过滤器在本模块） -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-webflux</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>

        <!-- 用户登录态 JWT（HS256，UserJwtService 在本模块）：api 编译期可见，impl/jackson 仅运行期实现 -->
        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-api</artifactId>
            <version>0.12.6</version>
        </dependency>
        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-impl</artifactId>
            <version>0.12.6</version>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-jackson</artifactId>
            <version>0.12.6</version>
            <scope>runtime</scope>
        </dependency>

        <!-- Swagger / OpenAPI（OpenApiConfig 在本模块） -->
        <dependency>
            <groupId>org.springdoc</groupId>
            <artifactId>springdoc-openapi-starter-webflux-ui</artifactId>
            <version>${springdoc.version}</version>
        </dependency>

        <!-- 可观测：健康 / 指标 / Prometheus -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>
        <dependency>
            <groupId>io.micrometer</groupId>
            <artifactId>micrometer-registry-prometheus</artifactId>
        </dependency>

        <!-- 多租户上下文跨线程传播：TenantContext 是 ThreadLocal（MyBatis 拦截器只能同步读），
             而 WebFlux 主链路会切到 boundedElastic 执行阻塞 JDBC。context-propagation 提供
             ThreadLocalAccessor SPI，配合 Hooks.enableAutomaticContextPropagation() 在线程边界还原。
             虽由 micrometer-observation 传递可得，仍显式声明——它是租户隔离正确性的直接依赖，
             不能寄望于传递链上游哪天不再引它。Boot 不托管该坐标，版本钉在根 pom。 -->
        <dependency>
            <groupId>io.micrometer</groupId>
            <artifactId>context-propagation</artifactId>
            <version>${context-propagation.version}</version>
        </dependency>

        <!-- 会话持久化：HikariCP 连接池 + MySQL 驱动。
             用 HikariCP 而非 spring-boot-starter-jdbc，避免引入 spring-jdbc 触发
             DataSourceAutoConfiguration（否则下游需配 spring.datasource.url 或排除它）。 -->
        <dependency>
            <groupId>com.zaxxer</groupId>
            <artifactId>HikariCP</artifactId>
        </dependency>
        <dependency>
            <groupId>com.mysql</groupId>
            <artifactId>mysql-connector-j</artifactId>
        </dependency>
        <dependency>
            <groupId>redis.clients</groupId>
            <artifactId>jedis</artifactId>
        </dependency>

        <!-- 业务持久层 ORM：MyBatis-Plus（版本对齐 customer-admin-server 的 3.5.7）。
             刻意用 mybatis-plus（extension 聚合，非 mybatis-plus-spring-boot3-starter），不触发官方
             MybatisPlusAutoConfiguration/DataSourceAutoConfiguration——本 starter 全部显式命名装配
             （CustomerWorkPersistenceConfig 独立 DataSource/SqlSessionFactory/SqlSessionTemplate + @MapperScan Ref 绑定），
             对 WebFlux 宿主与下游自有 MyBatis 环境零干扰。spring-jdbc 由其传递引入（SqlSessionTemplate 事务基础），
             此处显式声明以示意图；DataSourceAutoConfiguration 由宿主 spring.autoconfigure.exclude 关闭。 -->
        <dependency>
            <groupId>com.baomidou</groupId>
            <artifactId>mybatis-plus</artifactId>
            <version>${mybatis-plus.version}</version>
        </dependency>
        <!-- mybatis-plus 里的 mybatis-spring 是 optional(2.1.2, Spring5)，不传递；Boot 3(Spring 6) 需显式引入
             mybatis-spring 3.0.x（与官方 mybatis-plus-spring-boot3-starter 3.5.7 对齐用 3.0.3），
             提供 SqlSessionTemplate / @MapperScan / MapperScannerConfigurer 等 jakarta 版实现。 -->
        <dependency>
            <groupId>org.mybatis</groupId>
            <artifactId>mybatis-spring</artifactId>
            <version>3.0.3</version>
        </dependency>
        <dependency>
            <groupId>org.springframework</groupId>
            <artifactId>spring-jdbc</artifactId>
        </dependency>

        <!-- 长期记忆 Mem0/ReMe 客户端依赖 -->
        <dependency>
            <groupId>com.squareup.okhttp3</groupId>
            <artifactId>okhttp</artifactId>
        </dependency>

        <!-- 附件原始文件对象存储（customer-work.attachment.storage.type=minio 时启用；local 时该依赖仅在 classpath 不加载）。
             官方 MinIO Java SDK，传递引入 okhttp（与上面 Mem0/ReMe 的 okhttp 同源，版本由 Spring Boot 依赖管理统一）。 -->
        <dependency>
            <groupId>io.minio</groupId>
            <artifactId>minio</artifactId>
            <version>${minio.version}</version>
        </dependency>

        <!-- Nacos 配置中心 -->
        <dependency>
            <groupId>com.alibaba.nacos</groupId>
            <artifactId>nacos-client</artifactId>
            <version>${nacos.version}</version>
        </dependency>

        <!-- Skill 多存储目标：发布 SKILL.md 到 SFTP 目录（SftpSkillPublisher）。mwiede/jsch 是 JSch 的
             社区维护版，保持 com.jcraft.jsch 包 API 兼容；下游模块（admin）通过 starter 传递获得。 -->
        <dependency>
            <groupId>com.github.mwiede</groupId>
            <artifactId>jsch</artifactId>
            <version>${jsch.version}</version>
        </dependency>

        <!-- AgentScope Java 2.0 Harness（含 agentscope-core：Agent/Model/Tool/Middleware/State/Skill/RAG/Memory/Permission/Sandbox/Subagent/PlanMode/Compaction） -->
        <dependency>
            <groupId>io.agentscope</groupId>
            <artifactId>agentscope-harness</artifactId>
        </dependency>

        <!-- 2.0.0 GA 起，内置模型实现从 agentscope-core 拆成独立扩展模块（io.agentscope.core.model.*
             -> io.agentscope.extensions.model.{provider}.*，RC4 -> GA 升级踩的坑），ModelConfig 五个
             厂商分支都要显式声明依赖，版本由父 pom 引入的 agentscope-bom 统一管理，这里不写版本号。 -->
        <dependency>
            <groupId>io.agentscope</groupId>
            <artifactId>agentscope-extensions-model-dashscope</artifactId>
        </dependency>
        <dependency>
            <groupId>io.agentscope</groupId>
            <artifactId>agentscope-extensions-model-openai</artifactId>
        </dependency>
        <dependency>
            <groupId>io.agentscope</groupId>
            <artifactId>agentscope-extensions-model-anthropic</artifactId>
        </dependency>
        <dependency>
            <groupId>io.agentscope</groupId>
            <artifactId>agentscope-extensions-model-gemini</artifactId>
        </dependency>
        <dependency>
            <groupId>io.agentscope</groupId>
            <artifactId>agentscope-extensions-model-ollama</artifactId>
        </dependency>

        <!-- 会话/状态持久化：AgentStateStore 的 Redis / MySQL 实现（替代 1.x 的 session.redis/mysql） -->
        <dependency>
            <groupId>io.agentscope</groupId>
            <artifactId>agentscope-extensions-redis</artifactId>
        </dependency>
        <dependency>
            <groupId>io.agentscope</groupId>
            <artifactId>agentscope-extensions-mysql</artifactId>
        </dependency>

        <!-- 定时任务调度：把 Agent 注册为 XXL-JOB JobHandler，周期/启停/重试交给 XXL-JOB 控制台管理。
             版本由父 pom 引入的 agentscope-bom 统一管理，这里不写版本号。scheduler-common 是
             scheduler-xxl-job 自身 pom 里的 provided 依赖（不会传递），用到其 RuntimeAgentConfig /
             ScheduleConfig / AgentScheduler 等类需显式声明。 -->
        <dependency>
            <groupId>io.agentscope</groupId>
            <artifactId>agentscope-extensions-scheduler-common</artifactId>
        </dependency>
        <dependency>
            <groupId>io.agentscope</groupId>
            <artifactId>agentscope-extensions-scheduler-xxl-job</artifactId>
        </dependency>
        <!-- xxl-job-core：agentscope-bom 未管理其版本（仅托管 io.agentscope 自身制品），需显式声明。 -->
        <dependency>
            <groupId>com.xuxueli</groupId>
            <artifactId>xxl-job-core</artifactId>
            <version>3.3.2</version>
        </dependency>

        <!-- 分布式锁（DistributedLockExecutor，见 lock 包）：用 Redisson 而非已引入的 Jedis——
             加锁语义（可重入、CAS 安全解锁、连接失败即时抛错）不是简单 SET NX 能等价替代的，
             与"读缓存用 Jedis 直连"是两类不同的正确性要求，分开选型不复用同一客户端。
             agentscope-bom 未管理其版本，同 xxl-job-core，需显式声明。 -->
        <dependency>
            <groupId>org.redisson</groupId>
            <artifactId>redisson</artifactId>
            <version>3.52.0</version>
        </dependency>

        <!-- 长期记忆扩展：Mem0 / ReMe / 百炼 -->
        <dependency>
            <groupId>io.agentscope</groupId>
            <artifactId>agentscope-extensions-mem0</artifactId>
        </dependency>
        <dependency>
            <groupId>io.agentscope</groupId>
            <artifactId>agentscope-extensions-reme</artifactId>
        </dependency>
        <dependency>
            <groupId>io.agentscope</groupId>
            <artifactId>agentscope-extensions-memory-bailian</artifactId>
        </dependency>

        <!-- RAG 知识库扩展：简单内存 / Dify / 百炼 -->
        <dependency>
            <groupId>io.agentscope</groupId>
            <artifactId>agentscope-extensions-rag-simple</artifactId>
        </dependency>
        <dependency>
            <groupId>io.agentscope</groupId>
            <artifactId>agentscope-extensions-rag-dify</artifactId>
        </dependency>
        <dependency>
            <groupId>io.agentscope</groupId>
            <artifactId>agentscope-extensions-rag-bailian</artifactId>
        </dependency>

        <!-- 开发者工具箱的 JSON⇄YAML⇄XML 互转（DataFormatDevToolOps）。yaml 版已由 spring-boot 传递进来，
             xml 版没有，此处显式声明；版本交由 spring-boot-dependencies 管理，避免与 jackson-core 错版。 -->
        <dependency>
            <groupId>com.fasterxml.jackson.dataformat</groupId>
            <artifactId>jackson-dataformat-xml</artifactId>
        </dependency>

        <!-- 附件文档解析：Tika（pdf/doc/docx/ppt/pptx/html 文本抽取）。虽已由 rag-simple 传递到 classpath，
             此处显式声明并锁 3.3.0，防上游瘦身导致解析器缺失（poi-ooxml / pdfbox 亦由 rag-simple 传递，不重复声明）。 -->
        <dependency>
            <groupId>org.apache.tika</groupId>
            <artifactId>tika-core</artifactId>
            <version>3.3.0</version>
        </dependency>
        <dependency>
            <groupId>org.apache.tika</groupId>
            <artifactId>tika-parsers-standard-package</artifactId>
            <version>3.3.0</version>
        </dependency>

        <!-- 动态列结果导出 xlsx（sqlkit 的 XlsxExporter）。easyexcel 必须用 4.x（基于 POI 5.x）：
             上面的 agentscope-extensions-rag-simple 传递 poi-ooxml 5.5.1，若用 easyexcel 3.x（声明 poi 4.1.2），
             Maven nearest-wins 会裁出 poi 4.1.2 + poi-ooxml 5.5.1 的分裂 classpath，SXSSFWorkbook
             静态初始化直接 NoClassDefFoundError（实测踩过）。下面显式钉 poi/poi-ooxml 5.5.1 对齐两侧。 -->
        <dependency>
            <groupId>com.alibaba</groupId>
            <artifactId>easyexcel</artifactId>
            <version>4.0.3</version>
        </dependency>
        <dependency>
            <groupId>org.apache.poi</groupId>
            <artifactId>poi</artifactId>
            <version>5.5.1</version>
        </dependency>
        <dependency>
            <groupId>org.apache.poi</groupId>
            <artifactId>poi-ooxml</artifactId>
            <version>5.5.1</version>
        </dependency>

        <!-- 协议 / 网关 / 观测扩展：AG-UI / Higress / Studio / Nacos 提示词 -->
        <dependency>
            <groupId>io.agentscope</groupId>
            <artifactId>agentscope-extensions-agui</artifactId>
        </dependency>
        <dependency>
            <groupId>io.agentscope</groupId>
            <artifactId>agentscope-extensions-higress</artifactId>
        </dependency>
        <dependency>
            <groupId>io.agentscope</groupId>
            <artifactId>agentscope-extensions-studio</artifactId>
        </dependency>
        <dependency>
            <groupId>io.agentscope</groupId>
            <artifactId>agentscope-extensions-nacos-prompt</artifactId>
        </dependency>

        <!-- 仅取密码哈希工具（BCryptPasswordEncoder，用户账户密码加密），不引入
             spring-boot-starter-security 的自动装配（会拦截 WebFlux 端点）；版本随
             spring-boot-starter-parent 统一管理，与 customer-admin-server 保持一致。 -->
        <dependency>
            <groupId>org.springframework.security</groupId>
            <artifactId>spring-security-crypto</artifactId>
        </dependency>

        <!-- 证书解析（devtoolbox 系统工具的 cert_parse/cert_match）：CSR 与 PKCS#1/SEC1 私钥
             JDK 原生不认，靠 BouncyCastle；Provider 以实例方式使用，不注册进全局 Security。 -->
        <dependency>
            <groupId>org.bouncycastle</groupId>
            <artifactId>bcpkix-jdk18on</artifactId>
        </dependency>

        <!-- OpenTelemetry SDK + OTLP 导出器：把框架自带的 OtelTracingMiddleware（只依赖 otel-api，
             无 SDK 时全链路 no-op）真正接上 Collector/Tempo。opentelemetry-api 与
             opentelemetry-instrumentation:opentelemetry-reactor-3.1 已由 agentscope-core 传递引入，
             此处只补 SDK 侧。版本由根 pom 的 ${opentelemetry.version}(1.61.0) 统一管理，不写版本号。
             刻意不引 micrometer-tracing / opentelemetry-spring-boot-starter：前者要再引一层 bridge，
             后者自带一整套自动装配、与 Boot 托管版本互相打架。 -->
        <dependency>
            <groupId>io.opentelemetry</groupId>
            <artifactId>opentelemetry-sdk</artifactId>
        </dependency>
        <dependency>
            <groupId>io.opentelemetry</groupId>
            <artifactId>opentelemetry-exporter-otlp</artifactId>
        </dependency>

        <!-- Lombok -->
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <optional>true</optional>
        </dependency>

        <!-- 测试 -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>io.projectreactor</groupId>
            <artifactId>reactor-test</artifactId>
            <scope>test</scope>
        </dependency>
        <!-- 跨库门面（CrossDbGateways）单测用的内存库，仅测试期存在，不进运行时依赖 -->
        <dependency>
            <groupId>com.h2database</groupId>
            <artifactId>h2</artifactId>
            <scope>test</scope>
        </dependency>
        <!-- OTel 单测：InMemorySpanExporter 断言 span 结构与父子关系，不需要真起 Collector -->
        <dependency>
            <groupId>io.opentelemetry</groupId>
            <artifactId>opentelemetry-sdk-testing</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.jacoco</groupId>
                <artifactId>jacoco-maven-plugin</artifactId>
                <executions>
                    <execution>
                        <id>check-coverage</id>
                        <phase>verify</phase>
                        <goals><goal>check</goal></goals>
                        <configuration>
                            <rules>
                                <rule>
                                    <element>BUNDLE</element>
                                    <limits>
                                        <limit>
                                            <counter>LINE</counter>
                                            <value>COVEREDRATIO</value>
                                            <minimum>0.50</minimum>
                                        </limit>
                                        <limit>
                                            <counter>BRANCH</counter>
                                            <value>COVEREDRATIO</value>
                                            <minimum>0.40</minimum>
                                        </limit>
                                    </limits>
                                </rule>
                            </rules>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>

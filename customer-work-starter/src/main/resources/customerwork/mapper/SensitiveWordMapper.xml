<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.richard.fyoung.customerwork.safety.sensitiveword.mapper.SensitiveWordMapper">

    <!-- 按唯一键 word upsert：存在则更新类目/动作/启用/更新时间，不存在则插入（created_at_ms 仅首次生效） -->
    <insert id="upsert" parameterType="com.richard.fyoung.customerwork.safety.sensitiveword.entity.SensitiveWordEntity">
        INSERT INTO cw_sensitive_word (word, category, action, enabled, created_at_ms, updated_at_ms)
        VALUES (#{word}, #{category}, #{action}, #{enabled}, #{createdAtMs}, #{updatedAtMs})
        ON DUPLICATE KEY UPDATE category = VALUES(category), action = VALUES(action),
            enabled = VALUES(enabled), updated_at_ms = VALUES(updated_at_ms)
    </insert>

    <!-- 词表版本指纹：行数:最大更新时间戳。单行聚合，比每轮拉全表轻得多 -->
    <select id="selectFingerprint" resultType="java.lang.String">
        SELECT CONCAT(COUNT(*), ':', COALESCE(MAX(updated_at_ms), 0)) FROM cw_sensitive_word
    </select>
</mapper>

# 客户答复信息与来源留存

## 范围与调用链

H5 已有的参考线索和任务清单只存在于实时完成事件，刷新后历史接口只能恢复正文。调用方是客户 HTTP、SSE 与 WebSocket 的 `ChatTurnService`，下游是 `ChatTurnFinalizer`、`ChatLogService` 和消息 Store。此次把本轮答复信息与正文一起写入消息，沿用既有会话和工单历史查询；不额外发起模型或业务工具操作。

实际检索元数据由 RAG 工具在格式化前采集，采集器通过本轮 RuntimeContext 显式传递。工具文本标记继续作为兼容展示线索，不产生内部文档引用。历史响应仅投影客户可见字段，内部引用保留在存储中，后续原文预览须重新查当前权限。

## 实施与验证

1. 先验证历史丢失和本轮上下文未绑定，最小测试 2 项断言失败、0 错误；首轮实现通过。
2. 用真实工厂构建的 `retrieve_knowledge` 调用验证无文本标记的 Document。新增断言同时核对工具确实返回原文，避免把工具调用失败错当成来源采集失败。
3. 正文、终态、参考线索与任务清单同次 INSERT。旧消息保持没有采集信息，不推断为已完成或未命中。验证内存与真实 MySQL 往返、读取故障、跨消息隔离、现有受理和游标回放兼容性。
4. H5 验证历史、首连与重连补拉、当前答复合并、迟到响应及 390px 展示。原文预览和当前权限投影另有尚未完成的链路，不以展示来源标签代替完成验收。

已核对所有活动工作树和共享客服库，最高版本为 V26，本批分配 V27；未修改历史迁移。新增消息答复 JSON 列，同步完整镜像、可重复执行的手工升级脚本与迁移接管判定。两个结构快照已从独立空库执行全部迁移导出，客服端 50 张表、后台 91 张表；后台快照没有变更。

## 失败证据与定向回归

- 既有测试只验证正文和实时终态，没有验证刷新后的答复信息，也没有让真实 RAG 工具在原生上下文里返回元数据。修复前最小回归为 2 项断言失败；恢复旧工厂装配后，真实工具虽已返回原文，仍无法记录来源，1 项断言失败、0 错误。
- `ManagedKnowledge` 先校验向量结果属于实际请求的 PUBLIC 分区，再按实际物化记录检查 ACL、版本和文档修订。四种不一致的用例在修改前全部失败，修改后通过。此处的 PUBLIC 是客服端投影当前记录，不代表后台文档权限已实时同步。
- 在消息写入期间注入迟到计划事件，旧终态与落库快照不同，1 项断言失败、0 错误；修复后终态和正文使用同一份冻结快照。原生上下文同时覆盖正常结束、同步抛错后的恢复与嵌套调用。
- 数据库使用随机独立 MySQL 库，真实验证 JSON 往返、会话/工单读取、跨租户拒绝、旧消息兼容及损坏内容显式报错。直接把完整新镜像当作旧版的初次升级夹具出现重复列错误；已让夹具真实模拟旧版，并验证手工预加列后的幂等升级。
- 后端最终定向契约 82 项通过，覆盖 Agent 工厂、真实 ReAct 工具调用、AG-UI、消息 Store、HTTP 受理、WebSocket 和历史控制器。另一次含空库与旧库升级的回归 33 项通过；两者覆盖有重叠，不累加为独立用例数量。
- H5 新增状态与恢复断言的 RED 为 15 失败、20 通过；修改后 `Chat.test.ts` 35 项通过。完整 `message-delivery.e2e.ts` 11 项通过，类型与构建通过。缺失或 null 字段保留已知信息，明确的空数组清空旧值。

390px 浏览器夹具验证中断、错误、额度不足和 `MAX_ITERATIONS`；来源展开入口至少 44px，长计划与线索无横向溢出，最末片段标识可滚动到输入区上方，空格收起后焦点保留。三张最终截图已查看。浏览器使用本地 HTTP/WS 夹具，真实 MySQL、工具运行及租户校验由独立后端测试验证；不将夹具截图称为真实模型或业务服务联调。

## 未完成的关联范围

客户原文预览仍需接通可信消息引用和当前 PUBLIC 权限投影，历史引用失效后须停止读取。当前只展示参考线索，没有未接通的原文入口。助手计划中的完成状态只表示助手记录的进度，订单、退款和工单结果卡片继续按真实业务记录实施。物理设备软键盘验收尚未完成，390px 浏览器验证不替代该项。

## 完整门禁与审查

2026-09-12 最终 JDK 17 Maven 全模块 `BUILD SUCCESS`、exit 0：4,121 项，0 失败、0 错误、7 项环境条件跳过。starter 1,976 项（6 跳过）、app-server 177 项、channel 82 项、Admin 1,885 项（1 跳过）、gateway 1 项。跳过项依赖百炼、Nacos（4 项）、PaddleOCR 和外部真实 RAG 服务；Redis 持久化真实运行，未排除。

第一次完整后端门禁发现 Admin 跨库迁移集成测试仍期望 V26，而实际已迁移到 V27。已同步该固定版本断言，并补验 `cw_chat_message.answer_evidence` 确为 JSON 列，再冻结重跑全模块。最终 33 个源码、测试与结构文件的 SHA256 均与冻结清单相同。门禁通过现有测试数据库与连接环境变量，将可配置的持久化测试和 Admin Spring 上下文指向本批两个独占随机库；各迁移集成测试另用自行创建的临时库。最终两个门禁库已清理，没有修改共享开发库 Flyway history。

Admin 完整 255 项单测、204 项 E2E；H5 完整 68 项单测、11 项 E2E；两端类型与构建均 exit 0。第二次后端门禁只调整后端测试，未重复未变化且已通过的前端门禁。新增迁移与手工升级脚本逐字一致，所有 Admin 镜像文件完整，结构快照、Compose 配置、敏感凭据检查与 diff 空白检查通过。

独立安全审查覆盖消息/租户绑定、出站 JSON、模型文本与内部引用区分、RAG 物化授权检查及终态冻结，未发现本批新增可利用问题。审查为只读源码与调用链分析；真实 MySQL、ReAct 与传输验证由上述独立测试提供。审查结论不覆盖后续原文预览和后台撤权同步。

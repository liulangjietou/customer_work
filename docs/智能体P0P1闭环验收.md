# 智能体 P0/P1 闭环验收清单

## 1. 基线与范围

- 基线：`main@789894bb`。
- 交付分支：`codex/agent-p0-p1-completion`。
- 纳入范围：客服 Agent 运行时、Admin Agent 创作与控制面、渠道接入、运行时发布、评测与运营闭环。
- 支持拓扑：官方 K8s 双副本 Admin/App 部署，以及 MySQL、Redis、Nacos、MinIO 依赖。
- 状态含义：`TODO` 尚未完成，`DOING` 正在实现，`DONE` 已有自动化或运行证据证明闭口。

完成不是“源码里出现了类或字段”，而是对应入口、下游链路、迁移、前端、权限与自动化测试同时满足验收条件。

## 2. P0 阻断项

| 编号 | 验收条件 | 状态 | 完成证据 |
|---|---|---|---|
| P0-01 | Agent、渠道绑定、MCP 的编辑/停用/删除生成版本化变更或 tombstone；全部 Admin/Runtime Pod 失效旧缓存并 ACK，旧配置不可继续调用 | DONE | `RuntimePublishIntent`/可靠发布任务固化 UPSERT/REVOKE；`RuntimeAgentAccessState` + `AgentLifecycleMiddleware` 在请求链路校验 active/revision，tombstone 立即关闭运行时总闸；Agent/渠道/MCP 变更失效引用缓存；V81 及镜像 SQL；运行时修订、撤销、ACK 与缓存失效定向测试通过 |
| P0-02 | MCP 远程目标拒绝 SSRF、DNS/重定向绕过；stdio 仅在受控执行面运行；工具授权支持 tenant/agent/channel/subject，未知工具默认不可调用 | DONE | `McpSecurityPolicy` 对 URL 解析、每次 HTTP 请求、stdio 可执行文件/目录/参数/环境变量统一 fail-closed；`McpSubjectPolicy` + `SubjectToolAuthorizationMiddleware` 按可信主体授权并在 Admin UI 可配置；V82 及镜像 SQL；Starter 41、Admin/A2A 67、App/WS 21 项定向测试通过 |
| P0-03 | 统一可信调用上下文 `tenant + subjectType + subjectId + channel + session + agent`；终端用户可撤销；Admin/渠道记忆不串主体；退款审批绑定真实 session | DONE | `AgentInvocationIdentity` 覆盖六维可信身份并由 User/API Key/Admin/WS/A2A/内部任务入口建立；用户 `session_epoch` 支持撤销，`POST /api/customer/auth/revoke-sessions` 与 H5“退出所有设备”已接通，HTTP/WS/主体配额按租户复用同一会话校验；`AgentMemoryScope` 按 tenant/subject/agent/session 隔离 Admin/Harness 记忆，渠道外部用户使用哈希主体并校验 session 所有权；退款读取真实调用 session；V15/V83 及镜像 SQL；Starter 45 项及 Admin/渠道身份隔离定向测试通过 |
| P0-04 | 官方双副本拓扑下，internal cron 每个周期全局只产生一次任务执行记录 | DONE | `ScheduledTaskScheduler` 使用 Cron 精确 fireTime；`JdbcScheduledTaskClaimStore` 以 `(tenant_id, task_id, fire_time)` 数据库唯一键竞争执行权，未获得 claim 的 Pod 不执行；V84 及镜像 SQL；11 项调度/服务测试与 Admin 真库迁移通过 |
| P0-05 | 禁用 Skill 不能被新绑定或运行时加载；禁用已绑定 Skill 后引用 Agent 缓存失效 | DONE | Agent 绑定入口要求 Skill 存在且 `ENABLED`；`AdminAgentInstanceFactory` 仅装载启用 Skill；`SkillService` 停用后失效全部引用 Agent 实例缓存；Agent/Skill 服务与运行时工厂定向测试通过 |
| P0-06 | 每个模型调用冻结 provider、deployment、model、priceId、币种与单价；缺价进入 `UNPRICED`，不得匹配同名其他厂商价格 | DONE | `AttributedModel` 在真正订阅的底层 primary/fallback/policy/experiment 模型上发布不可变 `ModelCallAttribution`；`AgentCallTimingMiddleware` 按模型段冻结部署和价格，显式 `PRICED/UNPRICED`；`ModelPriceService` 强制 provider+model 精确取价；V16/V85、全量/升级/DBA 镜像 SQL；客服真库写入回读 17 项、Admin 定向 62 项与全 Spring 真库迁移 V80→V85 通过；调用详情前端展示实际部署与价目状态 |
| P0-07 | 流式 PII 脱敏在任何 delta 出站前完成，支持跨 chunk 命中与流结束 flush | DONE | `MaskingMiddleware` 按 text block 缓冲全部原始 delta，收到 block-end 或流完成后整段执行手机号/身份证/银行卡/邮箱/自定义正则脱敏，再释放唯一安全 delta；无界正则场景明确以隐私保证优先于逐 token 延迟，超 1 MiB 或过滤异常均 fail-closed；跨 chunk 手机号、缺 block-end 邮箱 flush、最终结果与敏感词组合链路 16 项测试通过 |
| P0-08 | 前端覆盖完整发布状态与门禁字段；登录态可完成 `BLOCKED → 原因 → 重评/豁免 → 继续发布` | DONE | 渠道绑定页覆盖 `BLOCKED` 等完整发布状态、门禁状态、候选哈希、九维版本绑定、评测运行及逐项失败原因；重评与带必填理由的紧急豁免分别受 `eval:run`/`eval:gate-override` 控制；Admin 前端生产构建及后端门禁/发布链路 34 项测试通过；独立空库 V1→V85、真实首次改密登录及 Playwright 验证通过，豁免持久化候选哈希/操作人/理由并进入 `PENDING/OVERRIDDEN`，重评进入 `PENDING/PENDING`，均继续可靠发布状态机 |

## 3. P1 完善项

| 编号 | 验收条件 | 状态 | 完成证据 |
|---|---|---|---|
| P1-01 | 同步、SSE、WS、AG-UI 使用统一终止信封，返回真实持久化 `messageId`、`finishReason`、`usage`、`traceId` | DONE | `ChatTurnService` 将模型流、BOT 消息持久化与终止结果收敛为唯一用例；`ChatTerminalCaptureMiddleware` 从受治理 Agent 事件累计真实多模型 usage 与 GenerateReason；同步响应、SSE done、WS chat_done、AG-UI RUN_FINISHED.result 映射同一 `ChatTerminalEnvelope`，无上游 trace 时为本轮补建 traceId；持久化失败不发伪终止；starter 12、app 16 项协议/失败语义测试及 H5 生产构建通过，类型契约与生产接口文档同步更新 |
| P1-02 | Admin 同一 `tenant + agent + session` 跨 Pod 串行；Plan/HITL 挂起态可被任意 Pod 确认；长期记忆并发更新有版本/CAS | DONE | Admin `ChatService` 以 tenant/agent/session 组合键接入 fail-closed Redisson permit 锁；`ai_plan_confirmation` 以 PENDING 条件更新冻结唯一终态，发起 Pod 轮询共享 Store 恢复本地 future；`ai_agent_memory.version` 与 JDBC/磁盘 Store CAS 拒绝旧工作副本覆盖；V86 及 DBA 镜像 SQL；两个 ChatService、两个 PlanConfirmationService、两个记忆工作副本并发回归及相关存储/服务共 58 项测试通过 |
| P1-03 | 转人工只保留一个权威工单状态机，创建、接单、结案和 SLA 不再双写分叉 | DONE | `HumanHandoffTools`、旧 `/handoffs` REST、智能路由回写和经营分析统一代理 `TicketService/cw_ticket`；旧 `HandoffStore`/SLA 配置不再注册生产 Bean/定时任务；`TicketSlaScheduler` 成为唯一 SLA；V17 与独立 DBA 镜像幂等归并旧表并保留审计事件；工单/路由/工具/装配 28 项、Controller 3 项、Admin 经营 SQL 5 项及客服真库迁移 6 项通过 |
| P1-04 | MCP/Toolkit 资源具备确定性关闭；后台 Agent 任务具备 owner、lease、heartbeat 和宕机接管 | DONE | `ManagedToolkit` 统一拥有并幂等关闭 MCP 客户端，覆盖注册/关闭竞态；Agent 缓存淘汰、修订替换、会话结束、编排临时 Agent 与容器销毁统一经 `AgentResourceCloser` 回收；`MybatisTaskRepository` 以 owner/lease/heartbeat 续租，过期任务跨租户扫描、CAS 领取并按原 tenant/subject/channel/session 重建执行上下文，重试耗尽确定性失败；`AgentTaskReplayCaptureMiddleware` 仅持久化 `agent_spawn(timeout_seconds=0)` 的原始提示词；V87 与 DBA 镜像 SQL；Starter 5、Admin 27 项资源/租约/迁移定向测试通过 |
| P1-05 | API Key 具备 keyId、哈希、scope、expiry、rotation、epoch；微信回调具备时间窗、nonce 去重、消息幂等和安全模式 | DONE | API Key 以 `keyId + SHA-256 hash + tenant + scopes + expiry + epoch` 解析，支持双 epoch 轮换和租户最小 epoch 吊销；认证、配额与后台调用均不再使用原始密钥作身份或计数键，生产环境拒绝旧明文配置。微信回调增加固定时间窗、Redis 原子 nonce/消息幂等、安全模式 AES 解密与 appId 校验，重放存储故障 fail-closed；V88 与 DBA 镜像 SQL、后台密钥加密/UI 配置同步完成。Starter/App 48、后台 35、渠道 37 项定向测试通过，部署与接口文档已更新 |
| P1-06 | 发布任务冻结 ACK 目标；MCP 凭据使用 SecretRef/KMS；配置可验签；maker-checker 与可靠审计/留存闭环 | DONE | 发布任务入队时冻结排序去重的实例集合，集合外 ACK 被拒绝且只有全体 APPLIED 才完成；MCP 敏感叶子提取为可轮换/到期/撤销的 SecretRef，Nacos 仅携带 `headersCipher`，执行前才解密；运行时配置在解密前校验 HMAC-SHA256 与可信 keyId；配置回滚/灰度改为 maker-checker，申请人不能自批，审批过期与执行超时均持久化，治理审计使用同步 hash chain 并保留十年；通用操作审计以 STARTED→COMPLETED 单向推进且异步接口在真实终止时补写。V89/DBA 镜像 SQL、固定双副本 StatefulSet 独立 ACK token/签名清单、Admin 生产构建、K8s YAML 解析及 25 项治理定向回归通过 |
| P1-07 | 模型健康状态作为动态路由 overlay，支持连续失败/恢复阈值、冷却、fallback、人工 override 和持久事件 | DONE | `ModelHealthStateMachine` 以连续失败/恢复阈值、冷却和 `AUTO/FORCE_HEALTHY/FORCE_UNHEALTHY` 计算有效状态；快照/探测/转换/过期/人工覆盖事件与 `HEALTH_OVERLAY` 可靠发布任务同事务提交；普通 failover、策略路由和在线实验统一剔除 `UNHEALTHY/RECOVERING`，实验故障臂回到健康基线而不跨臂；V90/DBA 镜像 SQL、独立 override 权限与 Admin UI 完成；Starter 31、Admin 92 项定向回归零失败（1 项真库测试因 MySQL 不可达跳过），Admin 生产构建与 K8s YAML 解析通过 |
| P1-08 | KnowledgeOps 支持文档源、增量/删除同步、checkpoint、lineage、ACL、新鲜度与质量；Agent 绑定不可变 KB/Skill 版本 | DONE | PUSH 来源以 requestId 幂等、checkpoint CAS 及单事务 UPSERT/DELETE/全量缺失删除推进不可变文档修订和 KB 快照，失败/质量不达标不推进；可信主体 ACL 在正文检索前 fail-closed，新鲜度、质量、运行与 lineage 可在管理端查看；KB/Skill 每次编辑生成不可变版本，Agent 保存冻结 versionId。V91/DBA 镜像 SQL、独立同步权限与前端知识运营抽屉完成；Starter 11、Admin 123 项相关定向测试零失败，Admin 生产构建、迁移镜像与 diff 检查通过 |
| P1-09 | Eval 数据集支持 CRUD、导入导出、审核、命名版本和 diff；实验启动前分别评测 control/treatment | DONE | 评测工作集支持 CRUD、批量导入导出，命名版本冻结不可变快照并以内容 hash 防冲突，独立审核权限与版本 diff 完成；模型实验必须绑定已审核 QUALITY 版本，ACTIVATE 前对 control/treatment 分别执行同一快照和 Judge，任一臂失败均不激活且两臂事实均持久化。V18/V92 及两份手工镜像 SQL、Admin 评测中心/实验面板完成；Starter/Admin 16 项定向测试、模块编译与 Admin 生产构建通过 |
| P1-10 | Replay 补齐模型参数、RAG、工具摘要和制品版本；只在隔离环境执行，默认 mock/dry-run，并输出差异 | DONE | 调用日志冻结最终模型参数白名单、输入 hash、RAG 命中引用、工具种类/输入形状/结果 hash，以及模型、KB、Skill/MCP/系统工具不可变版本指纹，不持久化凭据、RAG 正文或工具原始结果；清单默认 MOCK，执行端不存在 LIVE 模式且外部调用数恒为 0，DRY_RUN 还需服务端同时开启并声明 `isolated`，输出答案与制品差异。V19/V93 及两份手工镜像 SQL、独立重放权限和 Admin 安全重放抽屉完成；Starter/Admin 31 项定向测试、后端编译与 Admin 生产构建通过 |
| P1-11 | SLO 周期评估具备多副本租约；告警有 OPEN/ACKED/RESOLVED、恢复事件和可靠通知 | DONE | 独立双循环 Worker 不开启 Admin 全局调度；启用策略以数据库 CAS 租约周期领取，记录最近结果/错误并由租约过期支持宕机接管。告警以唯一活跃策略键收敛为 `OPEN → ACKED → RESOLVED`，只在足量健康样本下恢复，再次燃烧创建新周期；OPENED/ACKED/RESOLVED 事件与通知任务同事务落库，通知经独立租约、行锁和无限退避重试投递给 `slo:view` 用户。V94/DBA 镜像 SQL、独立 `slo:ack` 权限和 Admin 生命周期看板完成；33 项 SLO 定向测试、后端编译与 Admin 生产构建通过 |
| P1-12 | 模型调用金额可精确汇总到 call/session，并与日账单对账，业务结果提供单次自动解决成本 | DONE | 每个 MODEL 分段以调用时冻结的 provider/deployment/model/priceId/币种/单价和真实 usage 结算 14 位小数金额，显式区分 `SETTLED/UNPRICED/USAGE_MISSING/USAGE_INVALID`；调用与会话只在完整且单币种时给出可汇总金额，绝不把缺价、缺用量或混合币种伪造为 0。T+1 任务按自然日数据库锁串行、冻结 source max callId、从客服库不可变调用事实整日重建并写后回读校验，`/api/billing/reconciliation` 输出 `MATCHED/INCOMPLETE/STALE/MISMATCH`；业务结果展示会话成本及完整时的单次自动解决代理成本。V20/V95、两份手工镜像 SQL、Admin 调用/账单/业务结果页面完成；Starter/Admin 52 项定向测试、后端编译与 Admin 生产构建通过 |
| P1-13 | KnowledgeGap/badcase 具备 owner、状态、SLA、制品版本、复评、发布任务及上线效果观察闭环 | DONE | 两类原始信号共用 `ai_agent_improvement_case` 单一状态机，认领时冻结 owner/SLA，绑定 Agent 当前精确九维候选与目标回归用例；复评要求完整运行、目标用例通过、无新增回归、数据集稳定且候选版本一致，发布前再次拒绝候选漂移，并与可靠发布任务同事务入队。独立多副本数据库租约 Worker 只在任务全目标 `APPLIED` 后冻结 revision 与复发基线，按该 revision 的真实调用曝光及同类信号增量输出 `VERIFIED/INEFFECTIVE/INCONCLUSIVE`。V21/V96、两份手工镜像 SQL、KnowledgeGap/badcase 共用前端面板及独立权限完成；Starter/Admin 20 项定向测试、后端测试编译与 Admin 生产构建通过 |

## 4. 全局完成门槛

1. 每项变更包含其调用入口、下游链路和失败语义测试，不能只测独立类。
2. 新增 Admin Flyway 迁移同步到 `mysql/02-customer-admin/`；客服端表结构同时提供已存在表的升级迁移。
3. 后端六模块全量测试通过；本机只允许按项目约定排除 `RedisSessionPersistenceTest`。
4. `customer-admin-web` 与 `customer-work-app` 生产构建通过。
5. `git diff --check`、Java 质量审查、提交身份与无 AI/公司署名检查通过。
6. 功能分支推送并创建 OPEN/Ready-for-review PR 后，才可移除临时 worktree。

## 5. 最终分支验证（2026-08-24）

- Java 硬门：Corretto 17、Maven 3.6.3 离线执行
  `mvn -o clean test -Dtest='!RedisSessionPersistenceTest' -Dsurefire.failIfNoSpecifiedTests=false`，
  六模块全部 `SUCCESS`；共 3245 项，0 failure、0 error、139 skipped，仅按项目约定排除
  `RedisSessionPersistenceTest`。
- 前端硬门：`customer-admin-web` 与 `customer-work-app` 均执行 `npm run build` 成功；H5
  “退出所有设备”和 Admin P0/P1 治理入口均已进入生产构建产物。
- 覆盖率复核：Starter/App/Channel/Admin 行覆盖率分别为 67.8%/70.2%/65.5%/66.3%；新增高风险边界
  `KnowledgeSourceSyncCoordinator`、`KnowledgeBaseVersionService`、
  `ImprovementAutomationWorker`、`ConfigRollbackGovernedChangeExecutor` 行覆盖率分别为
  89.3%/75.5%/76.3%/87.5%。补测同时发现并修复首次 UPSERT/未知 DELETE 读取空 revision map
  导致的 NPE，checkpoint、质量门禁、墓碑、租户恢复与失败续跑均有回归测试。
- 迁移复核：Admin V81～V96 与 `mysql/02-customer-admin/` 镜像逐字节一致；客服 V15～V21
  均有 `mysql/01-agent-scope-customer-work/` 升级脚本，业务 SQL 一致，仅说明注释存在差异。
- 静态与安全复核：`git diff --check` 通过；新增 Java 无 `warn/debug/trace` 日志，无 XML `${}` 动态 SQL，
  无硬编码生产凭据、AI/公司署名或未收口 TODO；新增 Controller 均具备服务端权限，MCP stdio
  只经绝对真实路径白名单与清空环境的 `ProcessBuilder` 参数数组启动；未发现阻断交付的问题。

-- =============================================================================
-- Skill 管理菜单图标微调（V5 定的 Reading 语义偏"阅读"，跟"技能"关联度不高，换成更贴切的 Tools）
-- =============================================================================
-- V5 已应用过，不能再改（Flyway 靠校验和识别已应用迁移），新增一条迁移覆盖回填。

UPDATE `sys_permission` SET `icon` = 'Tools' WHERE `perm_code` = 'skill';

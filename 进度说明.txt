智能上下文压缩、RAG、Skill、MCP、可观测、Human-in-the-Loop、三层记忆体系等
待完善
1. 多 Agent 与分布式协作（缺口最大）
Pipeline 编排（core.pipeline）：SequentialPipeline / FanoutPipeline 多 Agent 串/并行
MsgHub（core.pipeline.MsgHub）：多 Agent 消息中心
子 Agent（tool.subagent.SubAgentProvider）：把子 Agent 当工具挂载（主从分层）
A2A + Nacos（core.a2a、core.nacos.a2a）：Agent Card 注册发现，跨服务 Agent 互通
RocketMQ（extensions.rocketmq）：异步消息解耦 + A2A over MQ
2. 运行时与调度
Scheduler（extensions.scheduler.AgentScheduler）：定时 / 调度 Agent
Runtime 沙箱：工具执行隔离（在独立项目 agentscope-runtime-java，本 jar 无 sandbox 包，需额外依赖）
优雅停机（core.shutdown.GracefulShutdownManager）：未显式集成
3. 记忆 / RAG 的其他后端
真实 Embedding RAG：rag.knowledge.SimpleKnowledge + core.embedding（向量库语义检索，本项目用的是自实现关键词版）
Mem0 / ReMe 长期记忆（memory.mem0、memory.reme）：只接了 memory + 百炼
RAG 其他集成：Dify / RAGFlow / Haystack（只接了 memory + 百炼）
4. Skill 进阶
其他技能仓库：Git / FileSystem / MySQL（只用了 Classpath 只读）
代码执行技能（SkillBox.codeExecution）、运行时加载技能（registerSkillLoadTool）、技能自进化/上传
5. 中断恢复与状态自动化
interruption 完整能力（core.interruption）：安全中断后无缝恢复（本项目只做了 interrupt() 触发，未做恢复）
PendingToolRecoveryHook / enablePendingToolRecovery：工具中断后恢复
StatePersistence / SessionManager：自动状态编排（本项目手动 saveTo/loadIfExists）
6. 可观测 / 评估 / 数据飞轮
Studio（core.studio）：可视化调试对接
原生 Tracing（core.tracing）：框架级 OpenTelemetry（本项目用的是 JSONL 导出）
Training（core.training）：RM Gallery 评估 + Trinity-RFT 强化学习
7. 模型层进阶
多模型 / 私有化兜底：OpenAI / Gemini / Anthropic / Ollama 切换 + fallback（只接了 DashScope）
GenerateOptions 高级：toolChoice / cacheControl（提示缓存）/ reasoningEffort / thinkingBudget / 联网搜索 / 多模态
8. 交互协议
AG-UI（core.agui）：标准 Agent-UI 事件协议
TTS（hook.TTSHook）：语音合成（实时多模态雏形）
优先级建议（生产价值 × 落地难度）：

高价值低风险：5 中断恢复/状态自动化、3 真实 Embedding RAG（SimpleKnowledge）、6 原生 Tracing(OTel)
高价值中等：1 子 Agent + Pipeline 多 Agent 编排、7 多模型 fallback + 提示缓存
需外部设施：A2A/Nacos、RocketMQ、Runtime 沙箱、Studio、Training（依赖 Nacos/MQ/独立项目